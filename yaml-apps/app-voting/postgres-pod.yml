apiVersion: v1
kid: Pod
metadata:
  name: postgres-pod
  labels:
    name: postgres-pod
    app: demo-voting-app
spec:
  containers:
  - name: postgres-pod
    image: postgres
    ports:
      - contanerPort: 5432